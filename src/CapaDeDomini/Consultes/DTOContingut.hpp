#include <string>
#include <chrono>

#include "../../definitions.hpp"

struct DTOContingut {
    std::time_t data_estrena;
    std::string tipus;
    std::string titol;
    std::string qualificacio;
    std::string info;
};
